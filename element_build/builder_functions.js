//Build a cute pressable button

//Build a wooden drawer with files inside which is the menu
function builder_drawer(links_obj) {
    //The links_obj should be an object as {linktext:link}
    //make the drawer
    for (l in links_obj) {
        //add l as the name of the textcontent
        //add links_obj[l] as the link
    }
}

//Build a typical connexion form
function builder_connex_form() {
    //needs email or login
    //needs password
}

//Build a typical inscription form
function builder_inscr_form(textfields) {
    //needs email and/or login
    //need name
    //need lastname
    //needs password
    //needs verification of password
}